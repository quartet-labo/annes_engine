<% content_for :title, "編集 - #{anne_admin_record_title(@record)}" %>

<section class="space-y-6">
  <div>
    <p class="text-sm font-medium text-slate-500"><%= @resource.label %></p>
    <h1 class="text-2xl font-semibold tracking-normal text-slate-950">編集</h1>
  </div>

  <%= render "form", resource: @resource, record: @record %>
</section>

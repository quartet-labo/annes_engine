<%= file_field_tag "#{name}[]", **attributes, multiple: true, accept: field.file_types.map(&:extension).join(",") %>
<% if local_assigns.fetch(:upload_selected, false) %><p class="inquiry-help">ファイルをもう一度選択してください。</p><% end %>

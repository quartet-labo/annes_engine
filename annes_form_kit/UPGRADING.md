# Upgrading AnnesFormKit

## 0.1.0

Initial package. There are no migrations, mount points or application initializers.
Use the dependency constraints of the consuming engine; publish this package before
dependent engines. The schema document version is independent of the gem version.

<% database_url = ENV["ANNE_LOYALTY_DATABASE_URL"].to_s.empty? ? ENV["DATABASE_URL"].to_s : ENV["ANNE_LOYALTY_DATABASE_URL"].to_s %>

test:
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
<% if database_url.empty? %>
  database: anne_loyalty_dummy_test
<% else %>
  url: <%= database_url %>
<% end %>

<% content_for :title, "新規登録 - #{@resource.label}" %>

<section class="space-y-6">
  <div>
    <p class="text-sm font-medium text-slate-500"><%= @resource.label %></p>
    <h1 class="text-2xl font-semibold tracking-normal text-slate-950">新規登録</h1>
  </div>

  <%= render "form", resource: @resource, record: @record %>
</section>

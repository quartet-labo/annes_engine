<h1>問い合わせフォーム管理</h1>
<p><%= link_to "フォームを追加", new_admin_form_path %></p>
<ul><% @forms.each do |form| %><li><%= link_to form.name, admin_form_path(form) %> (<%= form.key %>) <%= form.enabled? ? "受付有効" : "停止中" %></li><% end %></ul>

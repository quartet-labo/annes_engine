require_relative "test/dummy/config/application"
Rails.application.load_tasks

Rake::Task[:test].clear if Rake::Task.task_defined?(:test)
desc "Run the standalone intake tests"
task :test do
  ruby "bin/test"
end

task default: :test

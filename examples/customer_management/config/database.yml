<% database_url = ENV["CUSTOMER_MANAGEMENT_DATABASE_URL"].to_s.empty? ? ENV["DATABASE_URL"].to_s : ENV["CUSTOMER_MANAGEMENT_DATABASE_URL"].to_s %>

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
<% if database_url.empty? %>
  database: anne_customer_management_development
<% else %>
  url: <%= database_url %>
<% end %>

test:
  <<: *default
<% if database_url.empty? %>
  database: anne_customer_management_test
<% else %>
  url: <%= database_url %>
<% end %>
